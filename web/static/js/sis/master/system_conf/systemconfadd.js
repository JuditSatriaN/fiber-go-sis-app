// Function to handle if insert system config
function insertAction() {
    // Clear form insert system config
    clearFormInput();
    $("#modalUpsert #id").val("");
    $('#modalUpsert').modal('show');
}